window.addEventListener("load", function () {
  const inputPW = document.addEventListener("input", function (e) {
    const value = e.target.value;
    const checkLength = e.target.parentNode;
    if (value < 8) return;
  });
});
